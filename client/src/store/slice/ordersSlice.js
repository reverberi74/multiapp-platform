import { createSlice } from "@reduxjs/toolkit";

const initialState = {};

export const ordersSlice = createSlice({
  name: "orders",
  initialState,
  reducers: {
    // Qui metteremo le azioni
  }
});

export default ordersSlice.reducer;